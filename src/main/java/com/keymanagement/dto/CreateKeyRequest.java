package com.keymanagement.dto;

public class CreateKeyRequest {
    // Optional: Can be extended later to include key alias, metadata, etc.
    // For minimal implementation, empty request body is acceptable
}
